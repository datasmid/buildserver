---
- name: 'run ad-hoc playbook on test client'
  hosts: all
  user: root
  remote_user: vagrant
  sudo: yes
  gather_facts: yes

  pre_tasks:
    - include: tasks/client_pre_tasks.yml

  roles:
    - kbrebanov.virtualbox
    - ansible-eclipse

  tasks:
    - include: tasks/client_setup.yml
